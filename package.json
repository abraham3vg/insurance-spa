{
  "name": "insurance-spa",
  "version": "1.0.0",
  "description": "Mobile Insurance Survey SPA",
  "scripts": {
    "dev": "vite",
    "build": "chmod +x ./node_modules/.bin/vite && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}